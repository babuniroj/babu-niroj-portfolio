const nepaliDigits=n=>String(n).replace(/\d/g,d=>'०१२३४५६७८९'[d]);
function setCopyrightYear(){const now=new Date();const y=now.getFullYear()+(now.getMonth()>3||(now.getMonth()===3&&now.getDate()>=14)?57:56);const el=document.getElementById('copyrightYear');if(el)el.textContent=nepaliDigits(y)}
function updateCount(){const cards=[...document.querySelectorAll('#articleGrid .lekh-card')];const visible=cards.filter(c=>!c.hidden).length;const count=document.getElementById('articleCount');if(count)count.textContent=`${nepaliDigits(visible)} लेख`}
document.addEventListener('DOMContentLoaded',()=>{setCopyrightYear();const search=document.getElementById('articleSearch');if(search){search.addEventListener('input',e=>{const q=e.target.value.trim().toLowerCase();document.querySelectorAll('#articleGrid .lekh-card').forEach(card=>{card.hidden=q&&!card.dataset.search.includes(q)});const empty=document.getElementById('searchEmpty');if(empty)empty.hidden=[...document.querySelectorAll('#articleGrid .lekh-card')].some(c=>!c.hidden);updateCount()});updateCount()}if(window.lucide)lucide.createIcons({attrs:{'stroke-width':1.9}})});

// Article/listing mobile header navigation
document.addEventListener('DOMContentLoaded',()=>{const menu=document.querySelector('.header .menu');const nav=document.querySelector('.header nav');if(menu&&nav){menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open)});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{menu.setAttribute('aria-expanded','false');nav.classList.remove('open')}))}});
